# Note: Underscores in directory names seem to make in-traversable?

# Ignore top-level logs (typically npm-debug.log)
*.log

# Ignore the database (it'll make one on first run)
/data/*

# Ignore the server logs
/log/*

# Ignore minified .js files
/public/dist/javascripts/*

# Ignore minified .css files
/public/dist/stylesheets/*

# Ignore 'unknown use' files saved locally only in case of 404's
/public/stylesheets/unknown/*

# Ignore node.js runtime data
pids
*.pid
*.seed

# Ignore Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Ignore module (dependency) directory (https://npmjs.org/doc/faq.html#Should-I-check-my-node_modules-folder-into-git)
node_modules

# Users Environment Variables
.lock-wscript

# Ignore SSL certificate, certificate request, and private key
# To generate your own, do:
# openssl genrsa -out polljs-key.pem 1024
# openssl req -new -key polljs-key.pem -out certrequest.csr
# openssl x509 -req -in certrequest.csr -signkey polljs-key.pem -out polljs-cert.pem
# polljs-key.pem
# polljs-cert.pem
# certrequest.csr

# Ignore 'progress of the project' document
status.md

# Ignore Istanbul coverage reports
/test/coverage/*

# This is some deep, dark voodoo, man.
!.gitignore