{
  "name": "poll.js",
  "version": "0.0.1",
  "private": true,
  // Update fields below once project is public
  //"description": "Prototyping a polling service.",
  //"keywords": ["poll", "polling"]
  //"homepage": "http://..."
  //"license": {"license" : "SPDX LICENSE ID"}
  //"author": { "name" : "Ashton Eby", "email" : "", "url" : "" }
  "scripts": {
    // NOTE: -l logging is not done because of path conflicts; try to find a work-around?
    // Consider working `nohup` in conditionally?
    "prestart": "forever start --minUptime 1000 --spinSleepTime 1000 -e ./logs/database_errors.log -o ./logs/database_stdout.log ./bin/database_init_helper.js",
    // minUptime and spinSleepTime set to defaults to prevent warnings
    // Not fully sure what the two parameters do (!!!)
    "start": "forever --minUptime 1000 --spinSleepTime 1000 -e ./logs/server_errors.log -l ./logs/forever.log -o ./logs/server_stdout.log ./bin/server.js"
  },
  "dependencies": {
    // Fix the version numbers to specific releases
    "express": "~4.2.0",
    "static-favicon": "~1.0.0",
    "morgan": "~1.0.0",
    "cookie-parser": "~1.0.1",
    "body-parser": "~1.0.0",
    "debug": "~0.7.4",
    "jade": "~1.3.0",
    "mongodb": "*",
    "mongoskin": "*",
    "forever": "*"//,
    //"forever-monitor": "1.1",
    //"win-fork": "*"
  },
  "devDependencies": {
    "grunt": "*",
    "grunt-forever": "*",
    "grunt-contrib-jshint": "*",
    "grunt-contrib-uglify": "*",
    "grunt-contrib-nodeunit": "*",
    "grunt-contrib-clean": "*",
    "grunt-strip-json-comments": "*",
    "grunt-contrib-qunit": "*",
    "grunt-contrib-watch": "*",
    "grunt-contrib-concat": "*",
    "grunt-jsbeautifier": "^0.2.7",
    "grunt-lineending": "^0.2.2",
    "grunt-text-replace": "*"
  }
}