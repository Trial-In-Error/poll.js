{
  "name": "poll.js",
  "version": "0.0.1",
  // Update fields below once project is public
  //"description": "Prototyping a polling service.",
  //"keywords": ["poll", "polling"]
  //"homepage": "http://..."
  //"license": {"license" : "SPDX LICENSE ID"}
  //"author": { "name" : "Ashton Eby", "email" : "", "url" : "" }
  "private": true,
  "scripts": {
    // HARD-CODES PROJECT DIRECTORY!!!
    // Also, does NOT forever the database
    // Write a node.js script to do the prestart stuff
    // This also presumes windows without doing any checking
    // Lulz
    "prestart": "START \"\" mongod --dbpath C:/Users/Ashton/Documents/Code/poll.js/data",
    "start": "forever ./bin/server.js"
  },
  "dependencies": {
    // Split these into dependencies and devDependencies
    // Also, fix the version numbers to specific releases
    "express": "~4.2.0",
    "static-favicon": "~1.0.0",
    "morgan": "~1.0.0",
    "cookie-parser": "~1.0.1",
    "body-parser": "~1.0.0",
    "debug": "~0.7.4",
    "jade": "~1.3.0",
    "mongodb": "*",
    "mongoskin": "*",
    "grunt": "*",
    "grunt-forever": "*",
    "grunt-contrib-jshint": "*",
    "grunt-contrib-uglify": "*",
    "grunt-contrib-nodeunit": "*",
    "forever": "*",
    // Do we need forever-win? Test this!
    "forever-win": "*"
  }
}