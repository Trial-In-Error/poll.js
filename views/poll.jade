extends layout

block content
    h1= title
    p Hah! This is poll number #{poll.id}. It has a database hash of #{poll._id} and is owned by #{poll.owner}.


    #question
        h2 Question info
        p
            strong Poll ID:
            |  <span id='questionType'></span>
            br


    if globalExists
        script(src='dist/javascripts/client_poll.js')
    else
        script(src='/javascripts/client_poll.js')


//        <input type="text" data-clear-btn="true" name="text-nickname" id="text-nickname" value="" placeholder="Nickname">